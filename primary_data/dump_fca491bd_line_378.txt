{"step_index":216,"source":"MODEL","type":"CODE_ACTION","status":"DONE","created_at":"2026-06-13T05:50:34Z","content":"Created At: 2026-06-13T05:50:34Z\nCompleted At: 2026-06-13T05:50:37Z\nThe following changes were made by the replace_file_content tool to: c:\\Users\\stron\\Desktop\\Antigrabity\\7th_body\\app.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -1,4 +1,5 @@\n import os\n+import json\n import math\n import cv2\n import numpy as np\n@@ -41,6 +41,16 @@\n     \"guide\": \"primary_data/pose_guide.png\"\n }\n \n+# 에셋 실제 랜드마크 데이터베이스 로드\n+ASSET_LANDMARKS = {}\n+db_path = \"primary_data/asset_landmarks.json\"\n+if os.path.exists(db_path):\n+    try:\n+        with open(db_path, \"r\", encoding=\"utf-8\") as f:\n+            ASSET_LANDMARKS = json.load(f)\n+    except Exception as e:\n+        st.error(f\"랜드마크 데이터베이스 파일 로드 실패: {e}\")\n+\n # ==========================================\n # 2. 이미지 처리 및 과학적 정면 판별 엔진\n # ==========================================\n@@ -58,35 +58,31 @@\n         self.landmark = landmarks_dict\n \n def get_fallback_landmarks(w, h):\n+    \"\"\"\n+    분석 실패 시, sample_model.png의 실제 검출 랜드마크 정보를 환산하여 1대1 매치시킵니다.\n+    \"\"\"\n     landmarks_dict = {}\n-    landmarks_dict[0] = DummyLandmark(400/800, 155/1200)\n-    landmarks_dict[7] = DummyLandmark(460/800, 140/1200)\n-    landmarks_dict[8] = DummyLandmark(340/800, 140/1200)\n-    landmarks_dict[1] = DummyLandmark(415/800, 135/1200)\n-    landmarks_dict[2] = DummyLandmark(425/800, 135/1200)\n-    landmarks_dict[3] = DummyLandmark(435/800, 135/1200)\n-    landmarks_dict[4] = DummyLandmark(385/800, 135/1200)\n-    landmarks_dict[5] = DummyLandmark(375/800, 135/1200)\n-    landmarks_dict[6] = DummyLandmark(365/800, 135/1200)\n-    landmarks_dict[9] = DummyLandmark(410/800, 175/1200)\n-    landmarks_dict[10] = DummyLandmark(390/800, 175/1200)\n-    \n-    landmarks_dict[11] = DummyLandmark(550/800, 255/1200)\n-    landmarks_dict[12] = DummyLandmark(250/800, 255/1200)\n-    \n-    landmarks_dict[13] =\n<truncated 10052 bytes>\n   [lms[7].x * w, lms[7].y * h],\n+                [lms[8].x * w, lms[8].y * h]\n+            ])\n+            \n+            M = cv2.getAffineTransform(src_head_pts, dst_head_pts)\n+            \n+            head_path = f\"primary_data/{head_file}\"\n+            head_img = cv2.imread(head_path, cv2.IMREAD_UNCHANGED)\n+            if head_img is not None:\n+                head_layer = cv2.warpAffine(\n+                    head_img, M, (w, h),\n+                    flags=cv2.INTER_LINEAR,\n+                    borderMode=cv2.BORDER_CONSTANT,\n+                    borderValue=(0, 0, 0, 0)\n+                )\n+                head_rgb = head_layer[:, :, :3]\n+                head_alpha = np.expand_dims(head_layer[:, :, 3] / 255.0, axis=2)\n+                torso_layer[:, :, :3] = (head_rgb * head_alpha + torso_layer[:, :, :3] * (1.0 - head_alpha)).astype(np.uint8)\n+                torso_layer[:, :, 3] = np.maximum(torso_layer[:, :, 3], head_layer[:, :, 3])\n+                \n     if organ_type not in [\"skeleton\", \"muscles\", \"circulatory\", \"nervous\"]:\n         return torso_layer\n         \n@@ -388,7 +388,7 @@\n         if start_id in lms and end_id in lms:\n             pt_start = (lms[start_id].x * w, lms[start_id].y * h)\n             pt_end = (lms[end_id].x * w, lms[end_id].y * h)\n-            warped_seg = transform_limb_segment(path, image_shape, pt_start, pt_end, shoulder_width)\n+            warped_seg = transform_limb_segment(path, image_shape, start_id, end_id, pt_start, pt_end)\n             limb_layers.append(warped_seg)\n             \n     combined = torso_layer.copy()\n@@ -399,6 +399,7 @@\n         \n     return combined\n \n+\n def build_final_overlay(background_rgb, organ_layers, face_mask, selected_keys, selected_weights):\n     \"\"\"\n     다중 장기 레이어를 순서대로 결합하되, \n[diff_block_end]\n\nPlease note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown."}
