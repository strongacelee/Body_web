{"step_index":1302,"source":"MODEL","type":"VIEW_FILE","status":"DONE","created_at":"2026-06-14T06:37:41Z","content":"Created At: 2026-06-14T06:37:41Z\nCompleted At: 2026-06-14T06:37:41Z\nFile Path: `file:///c:/Users/stron/Desktop/Antigrabity/7th_body/scripts/07_verify_final_synthesis.py`\nTotal Lines: 140\nTotal Bytes: 6034\nShowing lines 1 to 140\nThe following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.\n1: import os\n2: import json\n3: import cv2\n4: import numpy as np\n5: \n6: def main():\n7:     print(\"보정 랜드마크 기반 전신(머리 및 몸통) 합성 로컬 시각적 검증 시작\")\n8:     \n9:     # 1. 파일 및 데이터베이스 로드\n10:     sample_path = \"primary_data/sample_model.png\"\n11:     db_path = \"primary_data/asset_landmarks.json\"\n12:     \n13:     if not os.path.exists(sample_path) or not os.path.exists(db_path):\n14:         print(\"Error: 샘플 모델 또는 랜드마크 DB 파일이 존재하지 않습니다.\")\n15:         return\n16:         \n17:     sample_img = cv2.imread(sample_path)\n18:     h, w = sample_img.shape[:2]\n19:     \n20:     with open(db_path, \"r\", encoding=\"utf-8\") as f:\n21:         landmarks_db = json.load(f)\n22:         \n23:     sample_lms = landmarks_db.get(\"sample_model.png\", {})\n24:     if not sample_lms:\n25:         print(\"Error: sample_model 랜드마크 정보가 DB에 없습니다.\")\n26:         return\n27:         \n28:     # 가상 랜드마크 101번과 102번 동적 계산 및 주입\n29:     p7 = np.array(sample_lms[\"7\"])\n30:     p8 = np.array(sample_lms[\"8\"])\n31:     c_ear = (p7 + p8) / 2.0\n32:     dx = p7[0] - p8[0]\n33:     dy = p7[1] - p8[1]\n34:     L = np.sqrt(dx**2 + dy**2)\n35:     if L > 0:\n36:         ux_up = dy / L\n37:         uy_up = -dx / L\n38:         ux_down = -dy / L\n39:         uy_down = dx / L\n40:         sample_lms[\"101\"] = (c_ear + np.array([ux_up, uy_up, 0.0]) * (L * 0.85)).tolist()\n41:         sample_lms[\"102\"] = (c_ear + np.array([ux_down, uy_down, 0.0]) * (L * 0.75)).tolist()\n42:   \n<truncated 3085 bytes>\n렌딩\n105:         if head_img is not None:\n106:             h_lms = landmarks_db.get(head_name, {})\n107:             if h_lms:\n108:                 src_head_pts = np.float32([[h_lms[str(idx)][0], h_lms[str(idx)][1]] for idx in head_indices])\n109:                 M_head, inliers = cv2.estimateAffinePartial2D(\n110:                     src_head_pts, dst_head_pts,\n111:                     method=cv2.RANSAC,\n112:                     ransacReprojThreshold=5.0\n113:                 )\n114:                 if M_head is not None:\n115:                     head_layer = cv2.warpAffine(\n116:                         head_img, M_head, (w, h),\n117:                         flags=cv2.INTER_LINEAR,\n118:                         borderMode=cv2.BORDER_CONSTANT,\n119:                         borderValue=(0, 0, 0, 0)\n120:                     )\n121:                     \n122:                     # 몸통 레이어에 머리 블렌딩 얹기\n123:                     head_rgb = head_layer[:, :, :3]\n124:                     head_alpha = np.expand_dims(head_layer[:, :, 3] / 255.0, axis=2)\n125:                     torso_layer[:, :, :3] = (head_rgb * head_alpha + torso_layer[:, :, :3] * (1.0 - head_alpha)).astype(np.uint8)\n126:                     torso_layer[:, :, 3] = np.maximum(torso_layer[:, :, 3], head_layer[:, :, 3])\n127:                     \n128:         # 최종 인물 이미지 위에 합성 (가중치 0.8 반투명 오버레이)\n129:         layer_rgb = torso_layer[:, :, :3]\n130:         layer_alpha = np.expand_dims(torso_layer[:, :, 3] / 255.0 * 0.8, axis=2)\n131:         combined_img = (layer_rgb * layer_alpha + combined_img * (1.0 - layer_alpha)).astype(np.uint8)\n132:         \n133:     # 4. 결과물 저장\n134:     out_path = \"visualizations/calibrated_full_body_test.png\"\n135:     cv2.imwrite(out_path, combined_img)\n136:     print(f\"전신 시각 검증 이미지 저장 완료: {out_path}\")\n137: \n138: if __name__ == \"__main__\":\n139:     main()\n140: \nThe above content shows the entire, complete file contents of the requested file.\n"}
